<%= flash[:notice] %>
<div class="container">
  <div class="row">
    <h1>マイページ</h1>
      <%if current_user.is_designer? == false%>
       <%="現在 #{current_user.name} 様は viewerモードです"%>
      <% else %>
       <%="現在 #{current_user.name} 様は designerモードです"%>
        <% if !current_user.nickname.present?%>
        <p> ・活動名orブランド名があれば登録してみましょう</p>
        <%end%>
        <% if !current_user.introduction.present?%>
        <p> ・自己紹介文を登録してみましょう</p>
        <%end%>
        <% if !current_user.image.present?%>
        <p> ・イメージ写真を登録してみましょう</p>
        <%end%>
        <% if (!current_user.homepage || !current_user.instagram).present?%>
        <p> ・HPやinstagramのリンクを登録してみましょう </p>
        <%end%>
      <% end %>
    <div class="col-md-4 col-md-offset-1">
      <%= attachment_image_tag current_user, :image, :fill, 10, 10, fallback: "no_image.jpg"%>
    </div>
    <div class="col-md-4 col-md-offset-1">
      <table class = "table">
        <tbody>
          <tr>
            <th class="table-heading">氏名</th>
            <td><%=current_user.name%></td>
          </tr>
          <tr>
            <th class="table-heading">活動名orブランド名</th>
            <% if current_user.nickname.present?%>
              <td><%=current_user.nickname%></td>
            <% else %>
              <td>（未設定）</td>
            <% end %>
          </tr>
          <tr>
            <th class="table-heading">紹介文</th>
            <% if current_user.introduction.present?%>
              <td><%=current_user.introduction%></td>
            <% else %>
              <td>（未設定）</td>
            <% end %>
          </tr>
          <tr>
            <th class="table-heading">メールアドレス</th>
            <td><%=current_user.email%></td>
          </tr>
          <tr>
            <th class="table-heading">HP</th>
            <% if current_user.homepage.present?%>
              <td><%=current_user.homepage%></td>
            <% else %>
              <td>（未設定）</td>
            <% end %>
          </tr>
          <tr>
            <th class="table-heading">instagram <i class="fab fa-instagram"></i></th>
            <% if current_user.instagram.present?%>
              <td><%=current_user.instagram%></td>
            <% else %>
              <td>（未設定）</td>
            <% end %>
          </tr>
          <tr>
              <%= link_to "商品をを新規登録する", new_product_path,class: "btn btn-primary"%>
              <%= link_to "登録情報を編集する", edit_user_path(current_user),class: "btn btn-primary" %>
            　<%= link_to "ユーザモードを変更する", user_mode_select_path(current_user),class: "btn btn-primary"%>
              <%= link_to "マイ商品一覧", products_path(user_id: current_user.id),class: "btn btn-primary"%>
              <%= link_to "お気に入り商品一覧", products_path(user_id: current_user.id),class: "btn btn-primary"%>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>


